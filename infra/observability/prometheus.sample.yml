global:
  scrape_interval: 15s
  scrape_timeout: 5s

scrape_configs:
  - job_name: discord-meeting
    metrics_path: /internal/metrics
    scheme: http
    static_configs:
      - targets:
          - meeting-service:3000
